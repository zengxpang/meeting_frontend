import React from 'react';

interface IBookedHistory {}
const BookedHistory = (props: IBookedHistory) => {
  return <div>BookedHistory</div>;
};

BookedHistory.defaultProps = {};

export default BookedHistory;
